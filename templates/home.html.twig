{% extends 'base.html.twig' %}

{% block title %}
    Bienvenue sur le mailleur réseau d'apparetements
{% endblock title %}
{% block body %}
    <div class="container">
        <div class="jumbotron mt-3">
            <h1 class="display-3">Bienvenue sur SymBNB !</h1>
            <p class="lead">Nous mettons en relations les voyageurs et les propriétaires qui veulent profiter d'oppoprtunités et gagner d'argent
            en proposant leur bien à la location !</p>
            <hr class="my-4">
            <p>Vous étes voyeageur ? Et vous voulez voir ou vous pourrez loger dans votre prochain voyage?</p>
            <p class="lead">
                <a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a>
            </p>
        </div>


    <h2 class="h1 my-5">Nos appartements stars</h2>
    <div class="row mt-5">
        {% for data in ads %}
            {% include 'ad/_ad.html.twig' with {'ad': data.announce} %}
        {% endfor %}
    </div>

        <h2 class="h1 my-5">Nos propriétaites stars !</h2>

        <div class="row mt-5">
            {% for data in users %}
            <div class="col">
                <div class="card border-secondary mb-3">
                    <div class="card-body">
                        <h4 class="crad-title">
                            {{ data.user.fullName }}
                        </h4>
                        <hr>
                        <div class="card-text">
                            <img src="{{ data.user.picture }}" alt="Avatar de {{ data.user.fullName }}" class="float-left">
                            <p>{{ data.user.introduction }}</p>
                            <a href="{{ path('user_show', {'slug': data.user.slug}) }}" alt="" class="btn btn-success float-right">En savoir plus</a>
                        </div>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>

    </div>

{% endblock body %}
